package classes.GameData 
{
	/**
	 * GameState is a global variable containing the active state of the game, as represented by an instance of GameModel.
	 * @author Gedan
	 */
	public var GameState:GameModel;
	
	{
		GameState = new GameModel();
	}
}